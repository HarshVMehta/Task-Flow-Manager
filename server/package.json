{
  "name": "ttm-server",
  "version": "1.0.0",
  "description": "Team Task Manager API Server",
  "main": "src/server.js",
  "scripts": {
  "dev": "node --watch src/server.js",
  "start": "npx prisma generate && npx prisma db push && node src/server.js",
  "prisma:push": "npx prisma db push",
  "prisma:generate": "npx prisma generate"
},
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
